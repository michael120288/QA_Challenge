version: 2.1
orbs:
  newman: postman/newman@5.2.4
jobs:
  build:
    executor: newman/postman-newman-docker
    steps:
      - checkout
      - newman/newman-run:
          collection: postman/TestAPI.postman_collection.json
          environment: postman/Local.postman_environment.json